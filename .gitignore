# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets
.env
.env.local
.env.*.local
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent / tool state — local to this machine, not part of the project
.agents/
.claude/
skills-lock.json

# Supabase CLI local cache
supabase/.temp/

# Wrangler local cache
.wrangler/

# Walkthrough recording artifacts (keep the script + EDIT_PLAN, not the media)
walkthrough/video/
walkthrough/frames/

# Playwright run artifacts
test-results/
playwright-report/
blob-report/

# Heavy binary media — untracked 2026-07-16 (was 606MB of the repo). Files
# stay on disk; keep them out of git. video/ = Remotion renders + sources,
# VILERAW/ = raw asset dumps.
video/
VILERAW/

# Entrance animation SOURCE (137MB GIF — over GitHub's 100MB hard limit).
# The generated frames it produces ARE committed (public/landing-sequence/);
# regenerate them with: npm run gen:landing-frames
# Root-anchored: git's ignorecase would otherwise swallow src/components/landing/.
/Landing/
.env.bak-jwt-key

# Print-ready NIIMBOT M2 label library (402 PNGs) + the generator's venv.
# Regenerate with: scripts/labels/.venv/bin/python scripts/labels/build_library.py
Product Labels/
scripts/labels/.venv/
scripts/labels/__pycache__/
